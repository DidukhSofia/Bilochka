import React, { useState } from 'react';
import Modal from 'react-modal';

function GiftModal({ isOpen, onClose, onSubmit }) {
    const [giftName, setGiftName] = useState('');

  return (
    <div>GiftModal</div>
  )
}

export default GiftModal